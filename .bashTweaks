#-----------------------------------------
#------ ALIAS SHORTCUTS START-------------

# GIT
alias gs="git status"
alias gc="git commit"
alias ga="git add"
alias gps="git push"
alias gpl="git pull"
alias gr="git remote"
alias grb="git rebase"
alias gl="git log"
alias gco="git checkout"
alias gb="git branch"

# Docker
alias d="docker"
alias dps="docker ps"
alias dl="docker logs -f --tail=1000"
alias dc="docker-compose"

# Auto attach existing tmux window
ssh () { /usr/bin/ssh -t "$@" "tmux attach || tmux new"; }
sshr () { /usr/bin/ssh "$@"; }

# VI shortcut
#alias v="vim --servername s --remote-tab"
# OSX Versioin
#alias v="mvim --servername s --remote-tab"

# OSX Convert FLAC to M4A (Apple Lossless)
alias doflac='for f in ./*.flac; do ffmpeg -i "$f" -c:a alac "${f%.*}.m4a"; done'

alias flushdns='dscacheutil -flushcache;sudo killall -HUP mDNSResponder;say flushed'

# Timestamp convert
alias ts='date -r'

alias fugpg="sudo launchctl unload /System/Library/LaunchDaemons/com.apple.ifdreader.plist && killall gpg-agent && gpg-agent --daemon"
# YK FIX:
# sudo launchctl unload /System/Library/LaunchDaemons/com.apple.ifdreader.plist

# shows all files in a directory including hidden files
alias ll='ls -lah'
alias la='ls -A'
alias l='ls -CF'

# Because I can
alias engage="play -n -c1 synth whitenoise band -n 100 20 band -n 50 20 gain +25  fade h 1 864000 1"

# clear the screen
alias c="clear"

# tar functions
alias wad='tar -czvf'
alias unwad='tar -xzvf'
alias md='mkdir -p'
alias ff='find . -iname'

# Show common commands
alias profileme="history | awk '{print \$2}' | awk 'BEGIN{FS=\"|\"}{print \$1}' | sort | uniq -c | sort -n | tail -n 20 | sort -nr"

# Logout of gnome
alias gnome-logout="gnome-session-quit --logout --no-prompt"

say() { if [[ "${1}" =~ -[a-z]{2} ]]; then local lang=${1#-}; local text="${*#$1}"; else local lang=${LANG%_*}; local text="$*";fi; mplayer "http://translate.google.com/translate_tts?ie=UTF-8&tl=${lang}&q=${text}" &> /dev/null ; }

#-----------------------------------------
#------HISTORY START----------------------

# Expand the history size
HISTFILESIZE=100000000
HISTSIZE=100000

# If you would like to prevent bash from saving any history (commented out by default).
# HISTFILESIZE=0
# HISTSIZE=0

#------------------------------------------
#------COMPRESSED VIEW START---------------

GIT_PS1_SHOWDIRTYSTATE=1
GIT_PS1_SHOWSTASHSTATE=1

# display compressed folder view preceded by time
RED="\[\033[0;31m\]"
GREEN="\[\033[0;32m\]"
BROWN="\[\033[0;33m\]"
BLUE="\[\033[0;34m\]"
PURPLE="\[\033[0;35m\]"
CYAN="\[\033[0;36m\]"
LGRAY="\[\033[0;37m\]"
YELLOW="\[\033[0;33m\]"
DGRAY="\[\033[1;30m\]"
WHITE="\[\033[1;37m\]"
RESET="\[\033[1;0m\]"
GIT='$(__git_ps1 "[%s]")'
PS1="[$LGRAY\t$RESET][$BLUE\u$RESET$CYAN@$RESET$PURPLE\h$RESET]: $GREEN\W$RESET$BROWN$GIT$RESET \$$RESET "


#------------------------------------------
#------WELCOME MESSAGE---------------------
# customize this first message with a message of your choice.
# this will display the username, date, time, a calendar, the amount of users, and the up time.

echo -ne "";hostname;
echo -ne "";uptime;
echo "";

#------------------------------------------
#------ENCRYPTION/DECRYPTION--------------
# requires gpg
# the proper way to use these functions is simply to enter "encrypt filename" or "decrypt filename"
encrypt () {
	gpg -ac "$1"
}

decrypt () {
	gpg -ad "$1"
}

des3 () {
	openssl enc -des3 -salt -in $1 -out $2
}

des3_d () {
	openssl enc -des3 -d -salt -in $1 -out $2
}

aes256 () {
	openssl enc -aes-256-cbc -salt -in $1 -out $2
}

aes256_d () {
	openssl enc -aes-256-cbc -d -salt -in $1 -out $2
}
